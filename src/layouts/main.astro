---
import Nav from "../components/nav.astro";

// export interface Props {
// 	title: string;
// }

// const { title, bgColor, description } = Astro.props;
---

<body class={`bg min-h-full h-full bg-slate-50`}>
	<Nav />
	<div class="container max-2xl: mx-auto">
		<slot />
	</div>
</body>
